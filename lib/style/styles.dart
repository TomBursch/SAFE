import 'package:flutter/material.dart';
import 'package:safe/style/colors.dart';

abstract class SafeStyles {
  static const TextStyle eventInfoText = TextStyle(
    color: SafeColors.portlandOrange,
  );
}
